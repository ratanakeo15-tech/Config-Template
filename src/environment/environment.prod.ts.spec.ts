import { EnvironmentProdTs } from './environment.prod.ts';

describe('EnvironmentProdTs', () => {
  it('should create an instance', () => {
    expect(new EnvironmentProdTs()).toBeTruthy();
  });
});
