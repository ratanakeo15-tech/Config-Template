import { EnvironmentIndicatorComponent } from './environment-indicator.component.ts';

describe('EnvironmentIndicatorComponentTs', () => {
  it('should create an instance', () => {
    expect(new EnvironmentIndicatorComponent()).toBeTruthy();
  });
});
