import { EnvironmentService } from './environment.service.ts';

describe('EnvironmentServiceTs', () => {
  it('should create an instance', () => {
    expect(new EnvironmentService()).toBeTruthy();
  });
});
