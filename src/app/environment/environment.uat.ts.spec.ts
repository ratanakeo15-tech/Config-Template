import { EnvironmentService } from "../core/service/environment.service.ts";


describe('EnvironmentUatTs', () => {
  it('should create an instance', () => {
    expect(new EnvironmentService()).toBeTruthy();
  });
});
